import 'package:flutter/material.dart';

class ListingsResultsPage extends StatelessWidget {
  const ListingsResultsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Wyniki wyszukiwania'),
      ),
      body: const Center(
        child: Text('Listings Results Page - To be implemented'),
      ),
    );
  }
}
